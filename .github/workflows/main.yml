on:
  schedule:
    // 14:13
    // UTC 中国时区 -8 （14-8 = 6）
    - cron:  '16 * * * *'

jobs:
  create_issues_actions:
     runs-on: ubuntu-latest
     name: create auto issues actions
     steps:
        - name: Checkout
          uses: actions/checkout@v2
          
        - name: create issues actions
          uses: ./

          with:
            tokens: ${{secrets.ACTIONS_DEMO_TOKEN}}
        
